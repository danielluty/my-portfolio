/* Fade in body */
(() => {
    document.getElementById('body').style.opacity = 1;

    // Array.from(document.querySelectorAll('.header-container span')).map(part => {
    //     part.style.letterSpacing = 0;
    // });
})();

